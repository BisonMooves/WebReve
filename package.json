{
  "name": "webreve-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "start": "npm start --prefix server",
    "build": "npm run build --prefix client",
    "build:client": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "init:db": "node server/scripts/createCollection.js",
    "mobile:sync": "npm run android:sync --prefix client",
    "mobile:apk": "npm run android:apk --prefix client",
    "mobile:run": "npm --prefix client exec cap run android",
    "mobile:open": "npm --prefix client exec cap open android"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
